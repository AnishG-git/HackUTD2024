package server

const (
	badlyFormattedRequest = "failed to decode request body, it may be malformed"
	encodingResponseError = "failed to encode response object into JSON"
	rKey                  = "reqBodyObj"
)
